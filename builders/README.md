# Builder Graph Templates

Shared Builder Graph templates live in this directory as `.buildergraph` files.

Eldiron scans this directory recursively from the Builder dock Treasury tab and applies selected templates directly to the current map selection.
