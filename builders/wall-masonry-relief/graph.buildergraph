name = "Wall Masonry Relief";
host = sector;

preview {
    width = 5.5;
    depth = 2.6;
    height = 0.3;
    surface = wall;
}

detail masonry {
    min = vec2(host.width * 0.04, host.depth * 0.08);
    max = vec2(host.width * 0.96, host.depth * 0.92);
    block = vec2(0.50, 0.28);
    mortar = 0.035;
    offset = -0.045;
    pattern = running_bond;
    material = STONE;
    tile_alias = stone;
};

output = [];
