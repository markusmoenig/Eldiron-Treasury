# Eldiron-Treasury
The treasury of Eldiron. Containing shared tile collections, Builder Graph templates, and more.

## Builders

Builder Graph templates live under `builders/` as `.buildergraph` files. Eldiron scans this directory recursively for the Builder dock Treasury tab, so templates can be organized into folders like `builders/walls/` and `builders/surfaces/`.
